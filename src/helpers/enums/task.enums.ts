export enum TaskStatus {
  Pending = 'Pending',
  InProgress = 'InProgress',
  OnHold = 'OnHold',
  Delayed = 'Delayed',
  Completed = 'Completed',
  Cancelled = 'Cancelled',
  Failed = 'Failed',
}

export enum TaskPriority {
  Low = 'Low',
  Medium = 'Medium',
  High = 'High',
}
